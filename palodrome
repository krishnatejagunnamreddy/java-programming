class Solution {
    public boolean isPalindrome(int x) {
        int dup =x;
        int rev=0;

        while(dup>0){
            int rem=dup%10;
            rev=rev*10+rem;
            dup/=10;
        }
        if(x==rev) return true;
        else return false;
    }
}
